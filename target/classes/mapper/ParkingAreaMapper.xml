<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.springboot.mapper.ParkingAreaMapper">

    <select id="selectAll" resultType="com.example.springboot.entity.ParkingArea">
        SELECT id, area_name AS areaName,
               available_spots AS availableSpots,
               occupied_spots AS occupiedSpots,
               hourly_rate AS hourlyRate,
               status
        FROM parking_area
        ORDER BY id
    </select>

    <!-- 保留原有的分页查询（可选） -->
    <select id="selectByPage" resultType="com.example.springboot.entity.ParkingArea">
        SELECT id, area_name AS areaName,
               available_spots AS availableSpots,
               occupied_spots AS occupiedSpots,
               hourly_rate AS hourlyRate,
               status
        FROM parking_area
        ORDER BY id
            LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 新增：支持关键词搜索的分页查询 -->
    <select id="selectByPageWithKeyword" resultType="com.example.springboot.entity.ParkingArea">
        SELECT id, area_name AS areaName,
        available_spots AS availableSpots,
        occupied_spots AS occupiedSpots,
        hourly_rate AS hourlyRate,
        status
        FROM parking_area
        <where>
            <if test="keyword != null and keyword != ''">
                area_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
        ORDER BY id
        LIMIT #{limit} OFFSET #{offset}
    </select>

    <!-- 保留原有的 count 查询（可选） -->
    <select id="count" resultType="int">
        SELECT COUNT(*) FROM parking_area
    </select>

    <!-- 新增：支持关键词搜索的 count 查询 -->
    <select id="countWithKeyword" resultType="int">
        SELECT COUNT(*)
        FROM parking_area
        <where>
            <if test="keyword != null and keyword != ''">
                area_name LIKE CONCAT('%', #{keyword}, '%')
            </if>
        </where>
    </select>

    <update id="updateById" parameterType="com.example.springboot.entity.ParkingArea">
        UPDATE parking_area
        <set>
            <if test="areaName != null and areaName != ''">
                area_name = #{areaName},
            </if>
            <if test="availableSpots != null">
                available_spots = #{availableSpots},
            </if>
            <if test="occupiedSpots != null">
                occupied_spots = #{occupiedSpots},
            </if>
            <if test="hourlyRate != null">
                hourly_rate = #{hourlyRate},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            update_time = NOW()
        </set>
        WHERE id = #{id}
    </update>

</mapper>